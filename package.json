{
  "name": "protoco-camp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "hand-on-sessions/*",
    "assignments/*"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.2"
  },
  "scripts": {
    "build": "tsc -b",
    "build:all": "npm run build --workspaces",
    "test": "jest"
  },
  "dependencies": {
    "ethers": "^6.13.2"
  }
}
